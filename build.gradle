plugins {
    id 'pl.allegro.tech.build.axion-release' version '1.13.1'
}

// logger.quiet("Invalid version $scmVersion.version")

afterEvaluate {
    scmVersion.tag {
        prefix = "ohoh"
    }

    logger.quiet("Vali version $scmVersion.version")
    version = scmVersion.version
}